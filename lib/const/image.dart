import 'package:flutter/material.dart';

class Logo {
  static Image logo = Image.asset(
    'logo/logo.png',
    width: 360,
  );

  static Container cLogo = Container(
      child: Image.asset(
    'logo/logo.png',
    width: 360,
  ));
}
